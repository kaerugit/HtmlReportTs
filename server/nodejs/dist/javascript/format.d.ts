export declare class Format {
    private static GetFormatType;
    /**
    * 値(DB)→表示(html)変換
    * @param value
    * @param formatString
    */
    static ParseFormat: (value: string, formatString: string) => any;
    private static changeDateValue;
}
